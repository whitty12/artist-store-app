<h1> Our Artists</h1>
<ul>
 <% @creators.each do |creator| %>
   <li><%= link_to "#{creator.full_name}", creator %></li>
 <% end %>
</ul>

<% if session[:role]== 'admin' %>
  <%= link_to "New artist", new_creator_path %>
<% end %>