<h1>Editing user</h1>

<%= render "form", user: @user %>

<br>

<div class="btn-group" role="group" aria-label="Basic example">
  <%= link_to "Profile", @user, class:"btn btn-secondary" %>

  <% if session[:role] %>
    <%= link_to "Back to users", users_path, class:"btn btn-secondary" %>
  <% end %>
</div>